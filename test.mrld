

extract import "libraries/std/lib.mrld";

func mc_factorial(n: number) {
    let n = Score::new( n );
    let result = Score::new( 1 );

    !{
        result *= n;
        n -= 1;

        (n > 0).then($);
    }!;

    result.print();
}

mc_factorial(4);
mc_factorial(6);
mc_factorial(7);




